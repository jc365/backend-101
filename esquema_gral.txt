


  ┌──────────────────────────────────────────────────────────────────────────┐
  │                                FRONTEND (React + Vite)                   │
  │                         ────────────────────────────────                 │
  │   📁 Tecnologías / Paquetes:                                             │
  │   └─ react, react-router-dom                                             │
  │   └─ firebase (Auth + Firestore client)                                  │
  │   └─ tailwindcss v3, axios, vite                                         │
  │                                                                          │
  │                                ▼                                         │
  │         [Login Component]                   [Dashboard Component]        │
  │         ┌──────────────┐                   ┌─────────────────────┐       │
  │         │ Google/GitHub│──┐                │ Mostrar pedidos     │       │
  │         │ Login in Firebase │              │ Crear pedido nuevo  │       │
  │         └──────────────┘  │                └─────────────────────┘       │
  └───────────────────────────┼──────────────────────────────────────────────┘
                              │
                              ▼6883a981fa9c04f269cc1883
                🔐  Firebase Authentication (Google / GitHub)
                     └─ Provee ID Token JWT tras login exitoso

                              │
                              ▼
          ┌────────────────────────────────────────────┐
          │         BACKEND (Node.js + Express)        │
          └────────────────────────────────────────────┘
          📁 Tecnologías / Paquetes:
          └─ express, dotenv, cors
          └─ firebase-admin         ← Verifica token de usuario
          └─ mongoose               ← Para conexión con MongoDB
          
          ┌────────────────────────────────────────────────────────┐
          │ Middleware: verifyFirebaseToken()                      │
          │ └─ Usa firebase-admin para verificar el idToken        │
          └────────────────────────────────────────────────────────┘

                    RUTAS API:
              📦 POST /api/pedidos    ← Crea pedido con UID
              📦 GET  /api/pedidos    ← Retorna pedidos del UID

                              │
                              ▼
                    📂 MongoDB Atlas (Base de Datos)
                    └─ Colección: pedidos
                       └─ Campos: userId, items[], fecha

